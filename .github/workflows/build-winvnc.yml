name: Build winvnc.exe

on:
  workflow_dispatch:
    inputs:
      configuration:
        description: Build configuration
        required: true
        default: Release
        type: choice
        options:
          - Release
          - Debug
      platform:
        description: Target platform
        required: true
        default: x64
        type: choice
        options:
          - x64
          - Win32
  push:
    branches:
      - main
    paths:
      - 'winvnc/**'
      - 'common/**'
      - 'rfb/**'
      - 'rdr/**'
      - 'omnithread/**'
      - 'lzo/**'
      - 'DSMPlugin/**'
      - 'ZipUnZip32/**'
      - '.github/workflows/build-winvnc.yml'
  pull_request:
    paths:
      - 'winvnc/**'
      - 'common/**'
      - 'rfb/**'
      - 'rdr/**'
      - 'omnithread/**'
      - 'lzo/**'
      - 'DSMPlugin/**'
      - 'ZipUnZip32/**'
      - '.github/workflows/build-winvnc.yml'

jobs:
  build:
    runs-on: windows-2022
    timeout-minutes: 120
    env:
      SOLUTION_PATH: winvnc/winvnc.sln
      PROJECT_NAME: winvnc
      VCPKG_ROOT: C:\\vcpkg
      VCPKG_DEFAULT_BINARY_CACHE: ${{ github.workspace }}\\vcpkg_cache
      CONFIGURATION: ${{ inputs.configuration || 'Release' }}
      PLATFORM: ${{ inputs.platform || 'x64' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Ensure NASM
        shell: pwsh
        run: |
          if (-not (Get-Command nasm -ErrorAction SilentlyContinue)) {
            choco install nasm -y --no-progress
          }
          nasm -v

      - name: Setup vcpkg
        shell: pwsh
        run: |
          if (-not (Test-Path "$env:VCPKG_ROOT\\vcpkg.exe")) {
            git clone https://github.com/microsoft/vcpkg.git "$env:VCPKG_ROOT"
            & "$env:VCPKG_ROOT\\bootstrap-vcpkg.bat" -disableMetrics
          }
          & "$env:VCPKG_ROOT\\vcpkg.exe" integrate install
          & "$env:VCPKG_ROOT\\vcpkg.exe" version

      - name: Cache vcpkg binary cache
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}\\vcpkg_cache
          key: ${{ runner.os }}-${{ env.PLATFORM }}-${{ env.CONFIGURATION }}-vcpkg-${{ hashFiles('winvnc/winvnc/vcpkg.json') }}

      - name: Install build dependencies (vcpkg classic mode)
        shell: pwsh
        run: |
          if ($env:PLATFORM -eq 'x64') {
            $triplet = 'x64-windows-static'
          } else {
            $triplet = 'x86-windows-static'
          }

          & "$env:VCPKG_ROOT\\vcpkg.exe" install `
            "zlib:$triplet" `
            "zstd:$triplet" `
            "libjpeg-turbo:$triplet" `
            "liblzma:$triplet" `
            "libsodium:$triplet"

      - name: Build winvnc.exe
        shell: pwsh
        run: |
          msbuild "$env:SOLUTION_PATH" `
            /t:$env:PROJECT_NAME `
            /m `
            /p:Configuration=$env:CONFIGURATION `
            /p:Platform=$env:PLATFORM `
            /p:VcpkgEnableManifest=false `
            /p:PostBuildEventUseInBuild=false

      - name: Validate build output (smoke test)
        shell: pwsh
        run: |
          if ($env:PLATFORM -eq 'x64') {
            $exe = Join-Path $env:GITHUB_WORKSPACE "winvnc\\winvnc\\x64\\$env:CONFIGURATION\\winvnc.exe"
          } else {
            $exe = Join-Path $env:GITHUB_WORKSPACE "winvnc\\winvnc\\$env:CONFIGURATION\\winvnc.exe"
          }

          if (-not (Test-Path $exe)) {
            throw "winvnc.exe not found at expected path: $exe"
          }

          $item = Get-Item $exe
          if ($item.Length -le 0) {
            throw "winvnc.exe exists but file size is zero"
          }

          $ver = $item.VersionInfo.FileVersion
          if ([string]::IsNullOrWhiteSpace($ver)) {
            throw "winvnc.exe file version is empty"
          }

          Write-Host "winvnc.exe path: $exe"
          Write-Host "winvnc.exe size: $($item.Length) bytes"
          Write-Host "winvnc.exe version: $ver"

      - name: Upload winvnc artifact
        uses: actions/upload-artifact@v4
        with:
          name: winvnc-${{ env.PLATFORM }}-${{ env.CONFIGURATION }}
          if-no-files-found: error
          path: |
            winvnc/winvnc/x64/${{ env.CONFIGURATION }}/winvnc.exe
            winvnc/winvnc/${{ env.CONFIGURATION }}/winvnc.exe
